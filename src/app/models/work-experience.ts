export interface WorkExperience {
  id: string;
  title: string;
  companyLogo?: string;
  companyName: string;
  currentRole: boolean;
  description: string;
  startDate: string;
  endDate: string;
  location?: string;
}
